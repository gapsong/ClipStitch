import expect from 'expect.js'
import {
    convertDurationtoSeconds,
} from '../lib/fetchTwitchChat'

describe('Example Node Server', () => {
    it('should return 200', (done) => {
        done()
    })
})
